{
  "name": "hide-azure-subscription-details",
  "version": "1.0.0",
  "private": true,
  "description": "Blurs Azure subscription IDs and subscription names on Azure portal pages.",
  "scripts": {
    "test": "jest --verbose"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
